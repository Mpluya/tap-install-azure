apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: rando-app
  namespace: tap-install
spec:
  serviceAccountName: tap-install-sadata.packaging.carvel.dev
  fetch:
    - git:
        url: https://github.com/Mpluya/repo-with-large-file-storage.git
        ref: origin/master
        subPath: test/dood
        secretRef:
          name: git-credentials
    - git:
        url: https://github.com/Mpluya/repo-with-large-file-storage.git
        ref: origin/master
        subPath: bro
        secretRef:
          name: git-credentials
  template:
    - ytt: {}
  deploy:
    - kapp:
        rawOptions:
          - --dangerous-allow-empty-list-of-resources=true